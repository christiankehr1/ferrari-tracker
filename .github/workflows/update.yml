name: Crawl and publish

on:
  schedule:
    # Hourly at :17 — off the top of the hour, where GitHub's scheduler is
    # busiest and runs get delayed the most.
    - cron: "17 * * * *"
  workflow_dispatch: # manual "Run workflow" button
  push:
    branches: [main]
    paths: ["web/**", ".github/workflows/update.yml"]

# Never let two crawls write data at the same time.
concurrency:
  group: crawl-and-publish
  cancel-in-progress: false

permissions:
  contents: write # commit data/
  pages: write # deploy site
  id-token: write

jobs:
  update:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Crawl AutoScout24
        run: python crawler/crawl.py

      # Between crawl and commit on purpose: the notified flags it sets ride
      # along in the same data commit. No secrets set = no-op, crawl unaffected.
      - name: Email new listings
        env:
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
          NOTIFY_TO: ${{ secrets.NOTIFY_TO }}
          NOTIFY_FROM: ${{ secrets.NOTIFY_FROM }}
          # deploy's page_url isn't available yet — that step runs later in this
          # job. The project-pages URL is deterministic, so derive it.
          SITE_URL: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/
        run: python crawler/notify.py

      - name: Commit data
        run: |
          git config user.name "ferrari-tracker"
          git config user.email "actions@github.com"
          git add data/
          # No changes is the normal case — prices rarely move. Don't fail.
          if git diff --staged --quiet; then
            echo "No data changes."
            exit 0
          fi
          git commit -m "data: crawl $(date -u +'%Y-%m-%d %H:%M UTC')"
          # Another run can land data between our checkout and our push, which
          # rejects ours as non-fast-forward. Both crawls read the same market and
          # ours is the later one, so replay it on top and let it win rather than
          # failing a publish over it.
          for attempt in 1 2 3; do
            if git push; then
              exit 0
            fi
            echo "Push rejected (attempt $attempt) — rebasing onto origin/main."
            git fetch origin main
            git rebase -X theirs origin/main || git rebase --abort || true
          done
          echo "Still couldn't push after 3 attempts."
          exit 1

      - uses: actions/setup-node@v5
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Build site
        working-directory: web
        run: |
          npm ci
          npm run build
          # Ship the fresh data alongside the built site.
          mkdir -p dist/data
          cp ../data/dashboard.json dist/data/

      - uses: actions/configure-pages@v6
      - uses: actions/upload-pages-artifact@v5
        with:
          path: web/dist
      - id: deploy
        uses: actions/deploy-pages@v5
